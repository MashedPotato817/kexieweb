# 后续工作计划（路线图）

> 本文件记录网站待办事项、已评估的方案结论与可选优化，作为后续 PR 的选题依据。
> 每完成一项，请将对应条目标记为已完成并附 PR 编号；新增事项按同样格式追加。
> 最后更新：2026-08-22

## 一、待办事项

### P0 · 立即可做

- [x] **og:image 分享配图**：用现有轨道图形生成 1200×630 分享图（Logo 征选前暂用），
  存入 `assets/images/og-cover.png`（随 assets/ 自动进部署清单），并在 `index.html` 补
  `og:image` 系列标签。负责人已确认暂用轨道 SVG。→ 已完成（PR #24）。

### 等 Logo 征选定稿（征集截止 2026-08-30 19:00）

- [ ] 中选 Logo 放入 `assets/images/`（从 `assets/logos/` 投稿暂存区由负责人复制提交）。
- [ ] 替换 `favicon.svg`。注意：favicon 在仓库根目录，**必须同步更新
  `deploy-pages.yml` 组装清单**（曾因遗漏导致线上 404，PR #23 的教训）。
- [ ] 导航栏 `brand-mark` 抽象占位换为正式 Logo。
- [ ] 用正式 Logo 重新生成 og:image，替换本轮的临时版。

### 等真实信息到位

- [ ] **招新信息发布**：确认时间、地点、报名链接后，替换「加入我们」区的
  「招新信息待发布」占位；报名链接须先登记到 `docs/kexie-mes/README.md`（外链核对强制）。
- [ ] **报名 CTA 按钮**（与招新信息同一 PR）：Hero 区高亮「立即报名」入口，跳转真实
  报名表单。无真实链接前不做——不得编造链接。
- [ ] **首场活动资料上线**：文件放入 `assets/downloads/`，将资源区三张占位卡
  （首场活动资料 / 项目展示模板 / 新成员入门指南）逐个替换为真实下载卡片。

## 二、已评估、暂缓或不做（防止反复讨论）

| 方案 | 结论 | 理由与重启条件 |
| --- | --- | --- |
| 数据 JSON 化 + fetch 渲染 | **暂不做** | ① `file://` 双击预览会因浏览器安全限制失效（README 承诺的零门槛预览报废）；② 页面内容将依赖 JS 才能显示，失去无脚本兜底；③ 当前仅 4 个部门卡片，数据量小收益低。**重启条件**：活动/项目卡片 ≥ 10 张，或出现「宣发同学网页端直接编辑数据」的真实需求（届时须设计 JSON 加载失败回退 HTML 的降级）。 |
| 群号一键复制 + Toast | **不需要** | 现有群号链接已直达 QQ 加群页，路径更短。 |
| 「改动须通过模块化脚本扩展」写入规范 | **不采纳** | 与「无构建、双击即开」的项目定位冲突。 |
| AGENTS.md 新增 Code Review/Safety Rules 章节 | **不采纳** | 现有机制（保护清单 + 边界检查 + 事实登记 + 人审合并）已覆盖同等约束。 |

## 三、后续可选

- [ ] **skill 扩充**：把「新增项目/资源卡」的 DOM 规范与路径、alt 检查要点并入
  `kexie-content-contribution` skill 的 references（不新建技能，避免碎片化）。
- [ ] **PROMPTS.md 招新宣传案例**：整理本项目建设中实际使用的 Prompt/Skill 案例，
  作为「AI + 工程实践」宣传素材。等有真实活动案例内容后再写。
- [ ] **图片规范**：宣发图统一 `.webp`、单图 ≤ 200KB、非首屏 `loading="lazy"`
  （当前全站仅二维码图，规范待真实图片到来时执行）。
- [ ] **长期维护习惯**：QQ 群二维码有效期巡检（过期用 `scripts/make-qr.py` 重生成）；
  每学期核对联系方式与部门信息；培养一位同学熟悉贡献流程，降低单人维护风险。

## 四、附录：外部审查建议对照（2026-08-22）

| 审查建议 | 对照结论 |
| --- | --- |
| 编写 CONTRIBUTING.md（P2） | 已存在且完善（零代码流程 + 提示词模板 + 隐私清单），无需重做 |
| check-asset-path 校验技能 | 已由 `validate-site.ps1` 机器强制（CI 拦截），超出 HTML 的 JSON 校验仅在数据 JSON 化重启时需要 |
| 非首屏图片 lazy | 已实现（二维码图，PR #21） |
| 移动端一键复制群号 | 已评估：现有链接直达加群页，无需开发（见上表） |
| 报名入口高亮 CTA | 方向采纳，被真实报名链接阻塞，随招新信息一并落地 |
| 数据 JSON 化（P0） | 已评估暂不做（见上表） |
| add-project-card 技能 | 采纳方向，并入现有 skill（见「后续可选」） |
| webp / 200KB / lazy 规范 | 采纳为规范，等真实图片到来执行 |
| PROMPTS.md 实践展示 | 采纳，等案例内容（见「后续可选」） |
